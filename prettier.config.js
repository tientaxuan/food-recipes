const config = {
  plugins: [require.resolve('prettier-plugin-tailwindcss')],
  singleQuote: true,
  printWidth: 80,
  endOfLine: 'auto',
  tabWidth: 2,
  useTabs: false,
};

module.exports = config;
